# Аюулгүй байдал ба продакшн бэлэн байдал

Энэ баримт нь системийг продакшнд гаргахаас өмнөх аюулгүй байдлын үнэлгээ, хийгдсэн засвар, үлдсэн ажлуудыг тодорхойлно.

## ✅ Одоо байгаа сайн талууд
- **SQL injection**: Бүх өгөгдлийн сангийн хандалт параметртэй query (`?`) ашигладаг тул SQL injection-оос хамгаалагдсан.
- **Нууц үг**: `bcrypt` (10 rounds)-аар хэшлэгдэж хадгалагдана. Хариултад password_hash хэзээ ч буцдаггүй (`toProfile`).
- **Нууцлал**: `.env.local`, `.env.production` файлууд `.gitignore`-д орсон, git-д track хийгддэггүй.

## ✅ Энэ удаа хийсэн засвар (hardening)
1. **XSS хамгаалалт** — RichTextEditor-ийн HTML агуулгыг (`agenda`, `decisions`, `notes`, даалгаврын тайлбар) `dangerouslySetInnerHTML`-ээр шууд харуулдаг байсныг [sanitize.ts](src/utils/sanitize.ts)-аар цэвэрлэдэг болгосон. Скрипт, `on*` event handler, `javascript:`/`data:` холбоос устгагдана.
2. **Payload хязгаар** — HTTP body лимитийг `1gb` → `40mb` болгож DoS-оос сэргийлсэн (`BODY_LIMIT` env-ээр тохируулж болно). 60MB payload → 413.
3. **Аюулгүй байдлын толгойнууд** — `X-Content-Type-Options`, `X-Frame-Options`, `Referrer-Policy`, продакшнд `Strict-Transport-Security`.
4. **Brute-force хамгаалалт** — нэвтрэх/бүртгүүлэх хүсэлтэд IP-ээр rate limit (1 минутад 10 оролдлого, дараа нь 429).
5. **Health endpoint** — продакшнд өгөгдлийн сангийн алдааны дэлгэрэнгүйг задлахгүй болгосон.
6. **Продакшн анхааруулга** — анхдагч admin нууц үг эсвэл хоосон DB нууц үгтэй үед серверийн лог дээр анхааруулна.

## 🔴 КРИТИК — заавал хийх ёстой (одоогоор дутуу)

### Сервер талын нэвтрэлт баталгаажуулалт (authentication/authorization)
**Одоогийн байдал:** `/api/*` бүх маршрут **нээлттэй** — токен/сесс шалгадаггүй. Эрхийн шалгалт зөвхөн клиент (browser) талд хийгддэг. Тиймээс сүлжээнд хандах чадвартай хэн ч дараахыг хийж болно:
- `GET /api/users` — бүх хэрэглэгчийн мэдээлэл унших
- `GET /api/messages/thread?userId=A&otherId=B` — бусдын хувийн зурвас унших
- `GET /api/personal-notes?userId=X` — бусдын хувийн тэмдэглэл унших
- `DELETE /api/users/:id`, `POST /api/leave-requests` г.м. — өгөгдөл устгах/өөрчлөх

**Шийдэл (санал болгож буй):**
1. `sessions` хүснэгт: `token` (санамсаргүй 32+ байт), `user_id`, `expires_at`.
2. Нэвтрэх/бүртгүүлэх үед токен үүсгэж буцаана. Клиент `Authorization: Bearer <token>` толгойгоор илгээнэ.
3. Сервер талд middleware: `/api/auth/*`, `/api/health`-ээс бусад бүх маршрутад токен шалгаж `req.user`-ыг тодорхойлно.
4. Эрхийн шалгалтыг сервер талд албадана:
   - Хэрэглэгч удирдах = зөвхөн admin
   - Зурвас/хувийн тэмдэглэл/амралт = зөвхөн эзэн нь (эсвэл admin)
   - `userId`-г query-ээс биш, токеноос авах

> Энэ бол хамгийн том, төвийг өөрчлөх ажил тул тусад нь анхаарч, туршиж хийх шаардлагатай. (Хэрэгжүүлэхэд бэлэн.)

## 📋 Продакшн deploy хийхийн өмнөх checklist
- [ ] **Сервер талын auth-ыг хэрэгжүүлэх** (дээрх критик зүйл)
- [ ] `.env.production` дээр:
  - [ ] `ADMIN_PASSWORD`-ыг хүчтэй утгаар солих (анхдагч `admin12345`-ыг битгий ашиглаарай)
  - [ ] `DB_PASSWORD`-ыг тавих (root/хоосон нууц үг битгий ашиглаарай)
  - [ ] `NODE_ENV=production`
- [ ] **HTTPS** — reverse proxy (nginx/Caddy)-аар TLS тохируулах. Апп нь HTTP дээр ажилладаг тул урд талд TLS заавал хэрэгтэй.
- [ ] Build: `npm run build` → `dist/`, дараа нь `NODE_ENV=production npm start`.
- [ ] MySQL: тусдаа (root биш) хэрэглэгч, зөвхөн шаардлагатай эрхтэйгээр.
- [ ] Өгөгдлийн сангийн нөөцлөлт (backup) тохируулах.
- [ ] Файл хавсралт base64-ээр DB-д хадгалагддаг тул хэмжээ хурдан өсдөг — их хэмжээний файлд объект санд (S3 гэх мэт) шилжүүлэхийг авч үзэх.

## Тэмдэглэл
- Файл хавсралтын хязгаар одоогоор ~30MB (40MB base64). `BODY_LIMIT` env-ээр тохируулж болно.
- `max_allowed_packet` серверийн эхлэлд 256MB болж автоматаар тавигдана (том хавсралт хадгалахад).
